version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install -g pnpm
        - pnpm install --frozen-lockfile
    build:
      commands:
        - pnpm --filter @area-code/admin build
  artifacts:
    baseDirectory: apps/admin/dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
