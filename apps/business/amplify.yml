version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install -g pnpm
        - pnpm install --frozen-lockfile
    build:
      commands:
        - pnpm --filter @area-code/business build
  artifacts:
    baseDirectory: apps/business/dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
